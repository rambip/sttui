Task 1: remove async function from the "copy" feature. Notify only when copy is done
Task 2: make sure that all the transcript parts are written to stdout, not just the last one
Task 3: Update the title and the style of the app. I want "Speak To TUI". Make sure that the non-capital letters are in a different color. Make sure to use the "Header" widget
Task 4: use numpy to compute the power of the sound sample from the last tick to this one. Use this information to create a horizonal volume animation to replace the spinner.
